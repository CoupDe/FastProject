import { FC } from "react";

interface ILoginListProps {
  date?: Date;
  label: string;
}

const Login: FC<ILoginListProps> = ({ date, label }) => {
  //const ss = useContext(ColorThemeContext);

  return (
    <>
      
      <div></div>
    </>
  );
};

export default Login;
